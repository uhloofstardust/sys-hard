{
  "name": "sys-hard",
  "version": "1.0.0",
  "description": "gui-based os hardening script",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "electron .",
    "start-flask": "python3 -m flask run -p 2312",
    "start": "concurrently \" npm run start-flask \" \"npm run electron\" ",
    "build": "electron-builder"
  },
  "author": "request denied",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "electron": "^26.2.2",
    "electron-builder": "^24.6.4"
  }
}
